import { makeStyles } from '@material-ui/core/styles';

export const launchCardStyles = makeStyles({
    root: {
      maxWidth: 345,
      marginBottom:20,
      marginTop:20
    },
    media: {
      height: 140,
    },
});